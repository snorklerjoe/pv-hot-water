"""This package contains all code used by the core software for the system.

The subpackages (aside from `common`) each contain a `__main__` module that
is used as an entry point for the Docker containers. The `common` package
contains utilities and database models that are used by the other subpackages.
"""
